# W108 — Supersession matrix after FAIL 2/2 — FROZEN

## Current W108 true-App

`tests/test_w108_thin_gtk_shell_app_desktop_e2e.py`
- REMOVE from GTK authority: exact 117 count; private bundle presence; `state` absence; `composition_complete`; W101 ledger parsing/count/schema; removed-method hasattr loop; private Workspace root topology.
- REPLACE with: one representative stable command per nine family with observable effect; Character Map insert+Undo; synthetic DnD/open; normal close/process/config.
- Static facts remain mandatory in W108/W101/W104 headless/source tests.

## W88 Authoring Bridge historical E2E

W108 may supersede only direct calls to App forwarding methods that W108 removes (`show_authoring_bridge`, `on_create_source_note_from_selection`, `on_insert_link_to_heading`). Replacement must use the stable W104 command path for user actions and preserve the exact W88 behavioral receipts. Do not restore the App forwarding methods.

## W105 Recent Workspaces historical E2E

Do not call removed `window.populate_recent_workspaces_menu()`.
Do not replace it with `window._components.workspace.host_runtime...`.
Trigger the published Workspace application behavior (`open_root()`/real recent-change path) and assert that the dynamic menu is reprojected, replaced idempotently and retains W105 semantics.

## Other historical W107→W98 tests

UNCHANGED unless Oracle Inventory Closure finds an exact physical facade that W108 is authorized to remove. No blanket private-to-public rewrite is authorized.
